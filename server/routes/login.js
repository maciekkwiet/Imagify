const express = require('express');
const router = express.Router(); //stworzenie modułu router

// router.post('/', (req, res) => {

//   console.log('body', req.body);
//   res.json(req.body);

// });

module.exports = router;
